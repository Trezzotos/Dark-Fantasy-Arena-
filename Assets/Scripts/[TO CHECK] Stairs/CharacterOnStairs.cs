using UnityEngine;

public class CharacterOnStairs : MonoBehaviour
{
    public bool isOnStairs = false;

    void Update()
    {
        // Puoi usare isOnStairs per attivare animazioni o modificare il movimento
        if (isOnStairs)
        {
            // Esempio: attiva animazione di salita
        }
    }
}
